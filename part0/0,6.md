``` mermaid
sequenceDiagram
    Client/Web Browser->>+Server: POST Request (new note in JSON format)
    note right of Client/Web Browser: The JavaScript handles more of the logic by converting the input as JSON, sending it to 
    note right of Client/Web Browser: the server and rendering it on the page, without needing all those other requests
    Server-->>+Client/Web Browser: 201 Status Code
