```mermaid
sequenceDiagram
    Client/Web Browser->>+Server: HTTP POST Request (New Note)
    Server->>+Client/Web Browser: HTTP 302 Response (URL Redirect)
    Client/Web Browser-->>Server: HTTP GET Request (Reload page)
    Client/Web Browser-->>Server: HTTP GET Request (CSS, JS and JSON data)
